# CLI Helpers

- `darktable-cli` remains the supported export CLI.
- `darktable-wb-resolve` is a small developer helper used by `darktableAI` to resolve truthful temperature-module params from image metadata.
- Treat `darktable-wb-resolve` as a prototype integration aid, not a stable end-user interface.
